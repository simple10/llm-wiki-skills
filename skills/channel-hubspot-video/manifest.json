{
  "keywords": [
    "hubspot",
    "cms",
    "video",
    "mux",
    "course",
    "player"
  ],
  "kind": "channel",
  "name": "channel-hubspot-video",
  "requires": {
    "credential": false,
    "network": [
      "play.hubspotvideo.com",
      "image.mux.com",
      "stream.mux.com",
      "*.mux.com"
    ],
    "ops": ">=1.96.0"
  },
  "stages": [
    "harvest",
    "process"
  ],
  "usage": "/channel-hubspot-video ticket=<id> stage=harvest|process",
  "v": 2,
  "venue": "hubspot-cms",
  "version": "1.7.0",
  "watch": {
    "defaults": {
      "every": "once",
      "harvest": {
        "access": "free",
        "assets": "download",
        "scope": "section"
      },
      "transcribe": {
        "when": "always"
      }
    },
    "dest": "sources/courses/{slug}",
    "note": "Keep `transcribe.when: always`. On this platform a page's text is typically a heading plus the player \u2014 the video IS the content \u2014 so without a transcript the capture carries almost nothing. Point the job at the section ROOT, not at a leaf: `harvest.scope: section` means under the job's own url, so from a leaf every sibling page is outside it. Set the site's selectors in the unit's `references/sites.json` after the first capture \u2014 every HubSpot site themes differently."
  }
}
