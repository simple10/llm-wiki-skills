{
  "keywords": [
    "frame",
    "share",
    "guest",
    "video",
    "assets",
    "deck",
    "host:frame.io",
    "host:*.frame.io"
  ],
  "kind": "channel",
  "name": "channel-frameio",
  "requires": {
    "bins": [
      {
        "bin": "yt-dlp",
        "source": "https://github.com/yt-dlp/yt-dlp",
        "installers": {
          "brew": "yt-dlp",
          "pip": "yt-dlp"
        }
      }
    ],
    "credential": false,
    "ops": ">=1.96.0"
  },
  "stages": {
    "harvest": {
      "sandbox_ref": "simple10/llm-wiki-skills:frameio/frameio.harvest"
    },
    "process": {}
  },
  "usage": "/channel-frameio ticket=<id> stage=harvest|process",
  "v": 2,
  "venue": "frameio",
  "version": "1.20.0",
  "watch": {
    "defaults": {
      "every": "once",
      "harvest": {
        "scope": "domain",
        "assets": "download"
      }
    },
    "dest": "sources/courses/{slug}",
    "note": "`harvest.scope: domain` is MANDATORY: a share's leaves sit under the share host, not under the job's url, so `page` excludes all of them, and `section` does too whenever the url is a folder inside the share rather than its root."
  }
}
