{
  "keywords": [
    "frame",
    "share",
    "guest",
    "video",
    "assets",
    "deck"
  ],
  "kind": "channel",
  "name": "channel-frameio",
  "requires": {
    "credential": false,
    "network": [
      "*.frame.io",
      "frame.io"
    ],
    "ops": ">=1.62"
  },
  "stages": [
    "harvest",
    "process"
  ],
  "v": 2,
  "venue": "frameio",
  "version": "1.14.4",
  "watch": {
    "defaults": {
      "harvest": {
        "scope": "domain"
      },
      "schedule": {
        "every": "once"
      }
    },
    "dest": "sources/courses/{slug}",
    "note": "`harvest.scope: domain` is MANDATORY: the host's intake applies the watch's scope prefix to every leaf this unit discovers, `page` rejects all of them while the run still exits 0, and `section` rejects them too whenever the watch URL is a folder inside the share rather than its root."
  }
}
